<ul>
    {{#each breakdown as |item key|}}
        <li>
            <div class="balance-details">
                <div class="label">{{ item.label }}</div>
                <div class="balance" data-balanceId="{{ item.wallet }}">{{{ item.total }}}</div>
            </div>
        </li>
    {{/each}}
</ul>